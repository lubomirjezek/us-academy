import { TrainingTypeToStringPipe } from './training-type-to-string.pipe';

describe('TrainingTypeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new TrainingTypeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
